name: Build & Push (Sandra React via Pi)

on:
  push:
    branches: ["main", "master"] # inclui master por segurança
  workflow_dispatch: {}

jobs:
  build:
    runs-on: [self-hosted, arm64] # Raspberry Pi runner
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build image (native arm64)
        run: |
          docker build -t ghcr.io/${{ secrets.GHCR_USERNAME }}/sandracamilo:latest .

      - name: Push image
        run: |
          docker push ghcr.io/${{ secrets.GHCR_USERNAME }}/sandracamilo:latest
